import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";

describe("Puzzle Wallet test", function () {
  it.only("", async function () {
    const proxy = await ethers.getContractFactory("PuzzleProxy");
    const wallet = await ethers.getContractFactory("PuzzleWallet");

    

  });
});
